for the simulator of domain_IndustryPlant ---incomplete---

brew install libmodbus
brew install CLIPS
download source code of clipsmm
cd to the download directory

download libtool from the sourcecode
go to the folder
run ./configure

go back to clipsmm folder
in autogen.sh change libtoolize to glibtoolize


brew install autoconf
brew install automake
brew install autogen

aclocal ; autoheader; automake ; autoconf
./configure
brew install shtool
autoreconf -i
randomly try the above


In VM-fusion ubuntu 16.04.4:
sudo apt-get install software-properties-common python-software-properties